.ipynb_checkpoints/
__pycache__/
*.pyc
.DS_Store

# Generated figure outputs (produced by running the scripts)
figures/

# Local cartopy coastline cache (see README; set CARTOPY_DATA_DIR)
.cartopy_cache/

# Data bundle tarballs (uploaded to Zenodo, not tracked in git)
*.tar.gz

# Plotting data lives on Zenodo (see data/README.md), not in git.
# Ignore the data payload but keep the folder's README tracked.
data/*
!data/README.md
